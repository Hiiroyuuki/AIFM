# AIFM — Python backend dependencies
# Install: pip install -r requirements.txt
#
# The React frontend is built with Node.js/npm — see README.md for setup.

fastapi>=0.110.0
uvicorn[standard]>=0.27.0   # [standard] bundles websockets + httptools
requests>=2.31.0             # used by the LLM model-list fetcher

# ── Desktop window mode (python start.py --app) ───────────────────────────
# PySide6 is optional: only needed for --app mode, which loads the React UI
# inside a native PySide6 + QWebEngineView window instead of the browser.
# Leave commented out unless you plan to use --app.
# PySide6>=6.5.0
